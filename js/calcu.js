'use strict';

var calc = function (start) {
    this.add = function (x) {
        
    };

    this.multiply = function (y) {
        
    };

    this.equals = function (callback) {
        
    };
}

new calc(0);
    .add(1);
    .add(2);
    .multiply(3);
    .equals(function (result) {
        console.log(" "+ result);
    });